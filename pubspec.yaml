name: flutter_bugsnag
description: A bugsnag reporter for flutter projects.
version: 0.2.0+1
homepage: https://github.com/pheedloop/flutter_bugsnag

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  package_info_plus: ^1.0.4
  device_info: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.9

flutter:
